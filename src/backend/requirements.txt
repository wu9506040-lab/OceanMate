# OceanMate AI Backend · v2.2 PoC 依赖
# 安装：pip install -r requirements.txt
# Python 3.11+ · Windows 需配 chcp 65001 防中文乱码

# === Web 框架 ===
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# === 数据模型 / 配置 ===
pydantic>=2.0.0
pydantic-settings>=2.0.0

# === 接口校验（BaseTool.validate_input 用）===
jsonschema>=4.21.0

# === AI 能力 ===
# DashScope Qwen（按需启用，无 Key 时自动降级 Mock）
# dashscope>=1.14.0

# === 向量数据库 ===
# Chroma 嵌入式（PoC 简化，不搭服务）
chromadb>=0.5.0

# === Day 14 P1-2：混合检索（BM25 + 中文分词）===
rank-bm25>=0.2.2
jieba>=0.42.1

# === 飞书 API ===
httpx>=0.27.0
# 长连接 SDK（lark-oapi）— 真实接入时事件接收主路径
# 提供 WS 客户端 + 自动签名/加密/鉴权，免公网回调
lark-oapi>=1.7.0
# pycryptodome 是 lark-oapi 加密事件的可选依赖（PoC 暂未启用 Encrypt Key）
pycryptodome>=3.20.0

# === 测试 ===
pytest>=8.0.0
pytest-asyncio>=0.23.0