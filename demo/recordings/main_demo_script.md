# OceanMate AI · 主 Demo 录屏脚本（4 分钟 · 单人录制版）

> **生成时间**：2026-08-16 凌晨
> **录制方式**：单方面录制（无评委），按此脚本逐字逐动作执行即可
> **Backend 版本**：Day 18 P0（chain_config + factory + KEA 关键词 + routers + TRA 自动 case 生成 + logger 修复）
> **测试覆盖**：434 passed
> **演示核心**：5 段数字员工闭环

---

## 0. 录制前准备（8 分钟 · 一气呵成做完）

### 0.1 后端启动（60 秒）

```bash
# 终端 1（专用 · 不要关）
cd E:/ai-pioneer/src/backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

**等待标志**：终端输出 `Application startup complete.` + `OceanMate AI 启动完成`
**健康检查**（另开终端 2）：
```bash
curl -s http://localhost:8000/api/health
# 期望返回：{"status":"ok","tools":[...]}
```

### 0.2 窗口布局（桌面准备 · 60 秒）

**目标**：录屏时只需 `Alt+Tab` 切换，不要拖动窗口。

| 窗口 | 位置 | 大小 | 用途 |
|------|------|------|------|
| **终端 1**（后端） | 屏幕左侧 1/4 | 半屏宽 × 全屏高 | 后端日志（录制中不切换） |
| **飞书桌面版** | 屏幕中央 | 占满 3/4 | 商户 + 运营对话 |
| **浏览器**（备用） | 屏幕右侧 1/4 | 半屏宽 × 全屏高 | mock_log + Dashboard 收尾 |

**飞书登录要求**：
- 用「飞行社企业」管理员账号（已有真实凭证）
- 商户身份：`ou_demo_merchant`（演示用占位）
- 运营 lead 身份：`ou_aa9ece53b9a503cf7007ce2d42021a1c`（私聊窗口提前打开）

**浏览器提前打开 3 个标签**：
1. `http://localhost:8000/api/feishu_mock_log`（后端日志）
2. `http://localhost:8000/api/chat` 测试页（备用，按 F12 调出开发者工具也行）
3. `https://oceanmate.feishu.cn/base/...`（OPA Dashboard 飞书多维表格 · 用真实飞行社企业账号）

### 0.3 飞书窗口初始化（60 秒）

**商户侧窗口准备**：
1. 飞书左侧栏 → 消息 → 找到商户群 `oc_6296625bae097b350d108e36150a869f`
2. 群里输入框预填测试消息（**不要按回车**）：
   ```
   BR Visa 拒付，错误码 13.1，怎么办？
   ```
3. **关键**：把这条草稿保留在输入框，等录屏 T0 时刻再回车

**运营 lead 侧窗口准备**：
1. 飞书左侧栏 → 消息 → 找到「智能助手」私聊（lead open_id=`ou_aa9ece53b9a503cf7007ce2d42021a1c`）
2. 输入框预填测试草稿（**不要按回车**）：
   ```
   列出待审核的 case
   ```

**关闭飞书通知横幅**（防止录屏中弹窗）：
- 设置 → 通知 → 关闭"新消息通知"

### 0.4 录屏工具设置（30 秒）

| 操作 | 快捷键 | 备注 |
|------|--------|------|
| 打开 Xbox Game Bar | `Win + G` | Win11 自带 |
| 点"捕获"按钮 | 顶部第二栏 | 或直接按下面快捷键 |
| 开始录制 | `Win + Alt + R` | 默认录全屏 + 系统声音 |
| 停止录制 | `Win + Alt + R` 再按一次 | 录完自动存到 `视频/捕获/` |

**录制前检查清单**：
- [ ] 系统时间正确（不要演示开始时显示 1970 年）
- [ ] 飞书通知关闭
- [ ] 后端 PID 20828 在跑（`netstat -ano | grep :8000`）
- [ ] 商户群草稿输入完成
- [ ] 运营 lead 私聊草稿输入完成
- [ ] 浏览器 mock_log 页签已开
- [ ] 录屏工具麦克风图标显示正常（右下角）

### 0.5 测试 1 次完整链路（120 秒 · 录制前必做）

**用 curl 模拟 T0，确认链路通畅**：
```bash
curl -X POST http://localhost:8000/api/chat \
  -H "Content-Type: application/json" \
  -d '{
    "query": "BR Visa 拒付，错误码 13.1，怎么办？",
    "user_id": "ou_demo_merchant",
    "merchant_context": {
      "merchant_id": "merchant_demo_001",
      "country": "BR",
      "channel": "Visa",
      "error_code": "13.1",
      "chat_id": "oc_6296625bae097b350d108e36150a869f"
    }
  }'
```

**期望返回**：含 `intent=payment_diagnosis` + `chain` 数组 + ticket_id（格式 `tkt_xxxxxxxxxxxx`）

**如果失败**：检查后端日志（终端 1），**不要开始录屏**

---

## 1. Demo 脚本（4 分钟 · 每步精确到秒）

### 🎬 开场（0:00 → 0:15 · 15 秒）

#### 步骤 1.0：按下录屏快捷键
- **操作**：`Win + Alt + R`
- **视觉确认**：屏幕右上角出现"正在录制..."小红点
- **等待**：1 秒（让 Game Bar 反应）

#### 步骤 1.1：开场介绍（口播）
- **窗口**：当前窗口（飞书商户群在前）
- **鼠标**：放在屏幕中央
- **口播稿（逐字）**：
  > 「这是 OceanMate AI —— 跨境支付商户的数字员工。**不是 AI 客服**，是机器人 + 人工协作的完整闭环。接下来 4 分钟，我演示一条真实商户咨询是如何走过 5 段流程的。」
- **时长**：13 秒（语速中等，不要赶）

#### 步骤 1.2：停顿 2 秒
- **操作**：闭嘴 2 秒
- **目的**：让观众看清屏幕布局（商户群 + 旁边有 mock_log 标签可切）

---

### T0 · 商户提问（0:15 → 0:45 · 30 秒）

#### 步骤 T0.1：发送商户问题
- **窗口**：商户群 `oc_6296625bae097b350d108e36150a869f`
- **操作**：
  1. 鼠标点击输入框（已预填草稿）
  2. 按 `Enter` 发送
- **视觉确认**：消息气泡出现在右侧（商户自己发出的样式）

#### 步骤 T0.2：等待 AI 处理（3-5 秒）
- **操作**：闭嘴，让 AI 真实处理
- **后台自动发生**（**不要说出来**，T2 才揭示）：
  - PDA Tool 诊断（chroma 召回 + LLM）
  - 自动派单 → TRA Tool → ticket_id 生成
  - KEA 召回相关 FAQ
  - 内部 briefing 私发给运营 lead
- **等待标志**：商户群内出现 AI 消息气泡

#### 步骤 T0.3：滚动到 AI 完整回复
- **操作**：鼠标滚轮向下，让证据链 + 建议处理 + 置信度全部可见
- **时长**：2 秒

#### 步骤 T0.4：讲解 AI 回复（口播）
- **鼠标**：悬停在"证据链（7 条）"行
- **口播稿（逐字）**：
  > 「看，AI 不只给答案 —— 把根因、证据链、处置建议全列出来。证据链 7 条来自 Chroma + 飞书多维表 + 商户配置。**注意：商户界面看不到下面的内部上下文**」

#### 步骤 T0.5：停顿 3 秒（让观众读证据链）
- **操作**：闭嘴 3 秒
- **鼠标**：悬停在"置信度：0.8"

---

### T1 · 触发反问机制（0:45 → 1:00 · 15 秒）

#### 步骤 T1.1：输入新问题（独立演示反问）
- **窗口**：商户群（**不是同一对话**的追问，是切到新会话避免 T0 已有完整诊断）
- **操作**：
  1. 鼠标点击飞书左侧栏「OceanMate 数字员工」
  2. 进入新私聊窗口
  3. 输入框输入（一个字符一个字符打）：
     ```
     NL iDEAL 支付失败，没收到错误码，怎么办？
     ```
  4. 按 `Enter`

> 💡 **提示**：因为商户侧对话系统已绑 T0 的上下文，简单追问会被识别为 payment_diagnosis 上下文继续。建议**另开一个私聊**（如直接搜「OceanMate」→ 进入纯新对话）。

#### 步骤 T1.2：等待 AI 反问（2-3 秒）
- **视觉确认**：AI 回复出现"🤔 您的问题我需要更多信息才能给出准确诊断。"

#### 步骤 T1.3：讲解反问机制（口播）
- **鼠标**：悬停在"country（哪个国家？"行
- **口播稿（逐字）**：
  > 「看，AI 不瞎编 —— 缺关键证据就反问，让运营或商户补全后再诊断。这是 Day 14 修的关键 bug：以前没 country/channel/error_code 时，AI 会一本正经胡编一个答案骗商户。」

---

### T2 · 运营侧 briefing 私发（1:00 → 1:25 · 25 秒）

#### 步骤 T2.1：切换到运营 lead 私聊窗口
- **操作**：`Alt+Tab` 切换（预设过这个窗口）
- **视觉确认**：屏幕显示 lead 私聊窗口

#### 步骤 T2.2：滚动到 briefing 消息
- **操作**：鼠标滚轮向下，找到最新一条带「💼 智能交接简报」的消息
- **视觉确认**：看到 5 段式 briefing 完整内容

#### 步骤 T2.3：讲解 briefing 创新点（口播）
- **鼠标**：从「💼 智能交接简报」标题 → 沿 5 段逐行悬停
- **口播稿（逐字）**：
  > 「**5 段式 briefing 私发给 lead —— 商户咨询界面看不到这些内部上下文**。运营接手即掌握全貌。这就是'机器人 + 人工'协作 —— 不是替代，是接力。」

#### 步骤 T2.4：停顿 3 秒让观众看清 briefing
- **操作**：闭嘴
- **鼠标**：悬停在「工单号：tkt_XXXX」行（具体 ticket_id 是当前真实生成的）

---

### T3 · 运营处理 + 关单（1:25 → 1:55 · 30 秒）

#### 步骤 T3.1：切回商户群
- **操作**：`Alt+Tab` 切回商户群窗口
- **视觉确认**：商户群在屏

#### 步骤 T3.2：模拟联系商户
- **操作**：手离开键盘，假装打电话（演示话术）
- **口播**：「我现在联系商户，问他要订单号 + 物流凭证……」

#### 步骤 T3.3：输入关单命令（NL 自然语言）
- **窗口**：商户群（同 T0）
- **操作**：
  1. 鼠标点击输入框
  2. 输入（用真实 ticket_id，从 T2 briefing 复制粘贴）：
     ```
     工单 tkt_03e97801f65b 已解决，已指导商户开启 3DS 并准备争议处理材料
     ```
  3. 按 `Enter`

> ⚠️ **关键警告**：
> - ticket_id 必须从 T2 实际看到的 briefing 里复制（每次跑会变）
> - **不要用「申诉」** —— 用「争议处理」（关键词冲突会让 T3 不进 TRA）
> - 准备好的备用 ticket_id：`tkt_03e97801f65b`（如 T2 没显示则用此）

#### 步骤 T3.4：等待 AI 关单响应（3-5 秒）
- **视觉确认**：AI 回复「工单 tkt_XXX 已记录为已解决 ✅ 状态：pending → closed 已自动生成案例 case_tkt_XXX_<ts> · 待人工审核」

#### 步骤 T3.5：讲解关单即沉淀（口播）
- **鼠标**：悬停在「已自动生成案例 case_tkt_XXX」行
- **口播稿（逐字）**：
  > 「**关键创新点 —— 关单即沉淀**。TRA 自动把这次解决记录成案例，confidence=0.85 落入 [0.7, 0.9) 区间进入待审核队列。不是直接进知识库 —— 半自动闭环要保留人工把关。」

#### 步骤 T3.6：复制 case_id 到剪贴板
- **操作**：`Ctrl+C` 复制 `case_tkt_XXX_<ts>`（后面 T6 审核用）

---

### T4 · KEA 半自动知识闭环（1:55 → 2:30 · 35 秒）

#### 步骤 T4.1：切到运营 lead 私聊
- **操作**：`Alt+Tab` 切到 lead 私聊（输入框已有「列出待审核的 case」草稿）

#### 步骤 T4.2：发送查询
- **操作**：
  1. 鼠标点击输入框
  2. 按 `Enter`（草稿已预填）
- **等待**：3-5 秒

#### 步骤 T4.3：讲解待审列表（口播）
- **视觉确认**：AI 返回「待审核案例（共 N 条）」+ 列表
- **鼠标**：悬停在第一条（就是 T3 生成的 case）
- **口播稿（逐字）**：
  > 「看，刚才 T3 生成的案例已经在这里了。半自动闭环的关键 —— AI 不能直接进知识库，必须人工把关。这是企业级 AI 应用的合规红线。」

#### 步骤 T4.4：停顿 2 秒

#### 步骤 T4.5：输入审核通过命令
- **操作**：
  1. 鼠标点击输入框
  2. 输入（3 种格式任选，用第 1 种最直观）：
     ```
     ✅ case_tkt_XXX_<ts>
     ```
  3. 按 `Enter`
- **替代格式**（如果上面失败）：
  - 「审核 case_xxx 通过」
  - 「approve case_xxx」

#### 步骤 T4.6：等待审核响应（3-5 秒）
- **视觉确认**：AI 返回「✅ case_xxx 已通过审核，已加入知识库 当前 faq_vec 共 N 条（之前 M 条）」

#### 步骤 T4.7：讲解飞轮（口播）
- **鼠标**：悬停在「faq_vec 共 N 条」行
- **口播稿（逐字）**：
  > 「知识库 +1。**注意数字 —— 之前是 M，现在是 N。这就是数据飞轮**：每审一条，知识库就长大一点。」

---

### T5 · 飞轮验证（2:30 → 3:00 · 30 秒）

#### 步骤 T5.1：切到商户侧 OceanMate 私聊
- **操作**：`Alt+Tab` 或飞书左侧栏点击 OceanMate 数字员工

#### 步骤 T5.2：输入同问题（验证飞轮）
- **操作**：
  1. 鼠标点击输入框
  2. 输入：
     ```
     BR Visa 13.1 又拒付了，商户很着急
     ```
  3. 按 `Enter`

#### 步骤 T5.3：等待 AI 召回（3-5 秒）
- **视觉确认**：AI 回复中包含「⚡ 命中知识库（上次同类问题已解决）」+ T3 审核的 case_id

#### 步骤 T5.4：讲解飞轮效应（口播）
- **鼠标**：悬停在「⚡ 命中知识库」行
- **口播稿（逐字）**：
  > 「**第二次问同问题，AI 直接复用上次已沉淀的 FAQ —— 数据飞轮效应**。注意 —— **T0 时 KEA 召回 0 条**（FAQ 还没这条），**T5 时召回 1 条**（刚审的 case 已沉淀）。同问题不再从零开始。这不是预设脚本，是 Chroma 真实召回。」

#### 步骤 T5.5：停顿 3 秒

---

### T6 · Dashboard 收尾（3:00 → 3:20 · 20 秒）

#### 步骤 T6.1：切换到浏览器 mock_log 页
- **操作**：`Alt+Tab` 切到浏览器
- **点击**：mock_log 标签
- **操作**：鼠标滚轮到顶部，按 `F5` 刷新
- **视觉确认**：看到后端真实处理日志（含 T0-T5 所有请求）

#### 步骤 T6.2：讲解 mock_log（口播）
- **口播稿（逐字）**：
  > 「后端日志记录所有真实处理 —— PDA → TRA → KEA 链式调用，briefing 私发 webhook，每一步都落地。」

#### 步骤 T6.3：切到 OPA Dashboard（飞书多维表格）
- **操作**：浏览器第三个标签（已预开）
- **视觉确认**：看到 4 张表

#### 步骤 T6.4：讲解数据规模（口播）
- **鼠标**：从工单池 → 错误码库 → 支付方式 → 路由规则 → 知识库
- **口播稿（逐字 · 配合鼠标停顿）**：
  > 「数据规模 —— **工单池 60 条、错误码库 117 条、支付方式 16 条、路由规则 10 条**，**总 203 条真实数据，全部从飞书多维表来，不是 mock**。**faq_vec 当前 9 条**（每次录屏会变）—— Chroma 自进化。**测试覆盖 434 passed**。」

#### 步骤 T6.5：停顿 2 秒

---

### 🎯 收尾（3:20 → 4:00 · 40 秒 · 含缓冲）

#### 步骤 END.1：切回商户群窗口（最后画面）
- **操作**：`Alt+Tab` 切回商户群

#### 步骤 END.2：结尾总结（口播）
- **口播稿（逐字）**：
  > 「**5 段数字员工闭环**：商户提问 → AI 解决+追问 → 拉人工 → 人工接 → 关单沉淀。
  >
  > **3 个亮点**：
  > 1. **不是 AI 客服，是 AI 数字员工** —— 商户界面看不到内部 briefing
  > 2. **数据飞轮自进化** —— 每次审核 +1，召回命中率涨
  > 3. **PoC 不是演示，是生产级** —— 飞书 WS 真接通 + 203 条真实数据 + 434 测试 + 真实 lead open_id
  >
  > **PoC 阶段能演示的才是功能**。完。」

#### 步骤 END.3：按录屏停止
- **操作**：`Win + Alt + R`
- **视觉确认**：小红点消失
- **等待**：1 秒（保存）

---

## 2. 录制中实时核对清单

每完成一个 T 时打 ✅：

| T | 视觉确认点 | ✅ |
|---|-----------|---|
| 开场 | 商户群窗口可见 | ☐ |
| T0 | 商户气泡 + AI 完整回复（7 证据链）| ☐ |
| T1 | AI 反问出现「🤔 您的问题我需要更多信息」| ☐ |
| T2 | lead 私聊有「💼 智能交接简报」5 段 | ☐ |
| T3 | AI 回复「工单已记录为已解决 ✅ 已自动生成案例 case_xxx」| ☐ |
| T4 | lead 私聊有「待审核案例」+「已通过审核，已加入知识库」| ☐ |
| T5 | AI 回复含「⚡ 命中知识库」+ T3 审核的 case_id | ☐ |
| T6 | Dashboard 4 张表 + 203 数据可见 | ☐ |
| END | 切回商户群画面 + 结尾口播 | ☐ |

**实时监控点**：
- 终端 1（后端）：每 5 秒扫一眼，有无红色报错
- 飞书：每步 `Alt+Tab` 后确认窗口对了
- 浏览器 mock_log：T6 时再切

---

## 3. 录制后处理

### 3.1 视频文件重命名

录屏默认存到 `C:\Users\zw\Videos\Captures\`（视频/捕获/）：

```bash
# 把 .mp4 拷到项目内
mkdir -p E:/ai-pioneer/demo/recordings/
cp "C:/Users/zw/Videos/Captures/主录制_*.mp4" E:/ai-pioneer/demo/recordings/main_demo_20260816.mp4
```

**注意**：`demo/recordings/*.mp4` 已在 `.gitignore`，不入版本控制

### 3.2 GitHub README 同步

提交的视频链接是给评委看的（GitHub README 嵌入 YouTube/B 站链接，或直接给 .mp4 文件）

### 3.3 飞书 AI 大赛报名模板提交

截止 **2026-08-16 22:00**（今晚 22 点）

需要填写：
- 录屏 URL（上传 B 站 / 飞书文档附件）
- 项目 GitHub URL
- 团队信息

---

## 4. 失败兜底方案

| 失败场景 | 兜底话术 |
|----------|----------|
| AI 处理超时（>10s） | 「真实 LLM 偶有延迟，等一下……」 |
| ticket_id 找不到 | 用备用 `tkt_03e97801f65b` |
| case_id 没生成 | 重新跑 T3，或用备用 `case_tkt_7801f65b_1786804654` |
| 后端挂了 | 终端 1 重启（`taskkill //F //PID 20828` 然后重启）|
| 飞书 WS 断了 | Poller 兜底（已配置 `FEISHU_POLL_CHAT_ID`）|
| 录屏没声音 | 检查右下角麦克风图标，Win+G 里点「音频」开麦 |

---

## 5. 关键时间戳速查

| 时刻 | 状态 |
|------|------|
| 0:00 | 开始录制 |
| 0:15 | T0 商户提问 |
| 0:45 | T1 反问 |
| 1:00 | T2 briefing |
| 1:25 | T3 关单 |
| 1:55 | T4 审核 |
| 2:30 | T5 飞轮 |
| 3:00 | T6 Dashboard |
| 3:20 | 收尾 |
| 4:00 | 停止录制 |

---

## 6. 真实跑通验证数据（2026-08-16 凌晨）

| 步骤 | 实际结果（`demo/recordings/full_t0_t9_v2.json`）|
|------|----------------------------------------------|
| T0 | intent=payment_diagnosis, conf=0.8, ticket=tkt_03e97801f65b |
| T1 NL iDEAL | intent=payment_diagnosis_clarify（触发反问） |
| T3 关单 | status=closed，TRA 自动生成 case_tkt_7801f65b_1786804654 |
| T4 list | 4 条待审，含刚生成的 case |
| T6 审核 | approved=True, faq_vec 5→9 |
| T5 飞轮 | 同问题召回刚审的 case |

> **注**：faq_vec 9 是因为脚本里多次测试累计，真实录一次只 +1。**重点是飞轮真实跑通**（T0=0 → T5=1）。

---

## 7. 8 项用户优化（已应用）

1. ✅ T1 改 NL iDEAL（独立演示反问机制）
2. ✅ T3 用「争议处理」避「申诉」关键词冲突
3. ✅ T3 加「已自动生成案例 case_xxx」行
4. ✅ 加 15s 开场介绍
5. ✅ T0 不剧透 briefing（T2 揭示）
6. ✅ T5 强调 T0=0 FAQ → T5=1 FAQ
7. ✅ T6 加路由规则 10 条，总 203 条
8. ✅ T1 15s + T6 20s + 收尾 20s 缓冲

---

## 8. 9 处关键修复（避免录屏翻车）

| # | 文件 | 修复 |
|---|------|------|
| 1 | `chain_config.py:59` | merchant_id fallback |
| 2 | `chain_config.py:79` | country fallback |
| 3 | `orchestrator/__init__.py:67-72` | TRA 注入 KEA + case_repo |
| 4 | `orchestrator.py:81-90` | KEA 关键词扩展 |
| 5 | `routers.py:367` | case_id 正则允许下划线 |
| 6 | `routers.py:651-654` | 删 reviewer 字段 |
| 7 | `tra/tool.py:477-509` | TRA 关单自动生成 case（Day 18 P0）|
| 8 | `tra/tool.py:518` | 删 except 内 logger 重绑定 |
| 9 | 录屏脚本 T3 | 「申诉」改「争议处理」|

**回归**：434 passed（0 失败）