# OceanMate AI · 主 Demo 录屏脚本（4 分钟 · 单账号复制粘贴版）

> **生成时间**：2026-08-16 凌晨（截止日当天）
> **演示载体**：飞书 OM AI 私聊（单窗口，全程不切换）
> **单账号限制**：飞行社账号 = 商户 + 人工客服（同一人）
> **Backend PID**：27436（重启后）· WS thread alive · 10 个 team open_id 全映射到 self-DM
> **核心创新**：会话状态延续 + force_intent 强制覆盖 + `[人工]` 前缀豁免 + session 找最近 ticket

---

## ⚠️ 录屏前 60 秒准备

| 操作 | 命令/动作 |
|------|-----------|
| 1. 后端已起 | `netstat -ano \| grep :8000` → PID 在 LISTENING |
| 2. WS 健康 | `curl http://localhost:8000/api/debug/ws_state` → `events_received=0` |
| 3. 飞书窗口 | 左侧栏 → OM AI（或 OceanMate 数字员工）→ DM |
| 4. 关闭通知 | 飞书设置 → 通知 → 关闭 |
| 5. **跑 dry-run 拿 ID**（下面） | `python /e/ai-pioneer/run_demo_dry.py` |
| 6. 开始录屏 | `Win + Alt + R`（右下角小红点出现） |

### 0.5 dry-run 拿真实 ID（30 秒）

```bash
PYTHONPATH=/e/ai-pioneer/src/backend PYTHONIOENCODING=utf-8 python /e/ai-pioneer/run_demo_dry.py 2>&1 | tail -20
```

输出末尾会打印：
```
[OUTPUT] 录屏脚本占位符替换：
   T0 ticket_id: tkt_xxxxxxxxxxxx
   T3 case_id:   case_tkt_xxxxxxxxxxxx_xxxxxxxxxx
```

⚠️ **这两个 ID 是后端生成的，每次跑会变**。
- 录屏时，**T3.2 的对话用真实 T0 飞书回复里的 ticket_id**（不是 dry-run 的）
- **T4.5 的对话用真实 T3.2 飞书回复里的 case_id**（不是 dry-run 的）

---

## 🎬 开场（0:00 → 0:15）

**口播**（13 秒）：
> 「这是 OceanMate AI —— 跨境支付商户的数字员工。**不是 AI 客服**，是机器人 + 人工协作的完整闭环。接下来 4 分钟，我演示一条真实商户咨询是如何走过 8 段流程的：**机器人自动诊断 → 自动派单 → AI 简报私发 → 反问机制 → 会话延续创建工单 → 转人工 → 人工接管 → 关单沉淀 → 数据飞轮**。」

**闭嘴 2 秒**（让观众看清飞书窗口布局）

---

## T0 · 商户提问 + AI 自动诊断（0:15 → 0:45）

### T0.1 商户发问（复制粘贴）

鼠标点飞书输入框 → Ctrl+V → Enter：

```
BR Visa 拒付，错误码 13.1，怎么办？
```

### T0.2 等待 AI 处理（3-5 秒，闭嘴）

### T0.3 你看到的回复（3 条机器人气泡，从上到下）

```
[气泡 1] 🤝 为了给您更专业的服务，我帮您转接 **财务团队-争议处理** 专家跟进。

📨 **工单号**：`tkt_xxxxxxxxxxxx`
⏱️ 预计 12h 内回复您。

💡 如有新信息（订单号 / 错误码 / 已开的 3DS 等），请直接回复给我，我会更新到工单里。
🤖 **【AI 交接简报 · 仅客服可见】**
━━━━━━━━━━━━━━━━━━━━
👤 **商户**：ID `merchant_demo_001`

❓ **问题类型**：拒付

� **AI 已诊断**：
- 渠道+错误码：**Visa 13.1**
- 根因：**持卡人/银行认为未收到商品**
- 置信度：**高**（80%）

🎯 **建议下一步**：补充物流凭证 + 申请 RDR
━━━━━━━━━━━━━━━━━━━━
📨 **工单**：`tkt_xxxxxxxxxxxx` | 优先级 **medium** | SLA **12h** （截止时间）
💬 **对话来源**：chat_id=`oc_xxxxx` · open_id=`ou_xxxxx`
```

**操作**：滚轮向下，让 3 条气泡都可见

### T0.4 口播（10 秒）
> 「看，AI 不只给答案 —— 把根因、证据链、处置建议全列出来。同一会话里还自动推了简报和工单确认。**注意商户界面只看到第 1 条气泡**，简报和工单确认虽然在同一会话但视觉上是分开的。」

**闭嘴 3 秒**（让观众读证据链）

> ⚠️ **复制工单号**：从第 1 条气泡复制 `tkt_xxxxxxxxxxxx`（下面 T3.2 用）

---

## T1 · 反问机制（0:45 → 1:00）

### T1.1 商户发问（复制粘贴）

```
NL iDEAL 支付失败，没收到错误码
```

### T1.2 等待 AI 处理（3 秒，闭嘴）

### T1.3 你看到的回复

```
🤔 您的问题我需要更多信息才能给出准确诊断。

缺哪个？麻烦提供一下：
- **国家**（哪个国家？）
- **支付渠道**（Visa / Mastercard / iDEAL / 其他？）
- **错误码**（错误码或具体现象）
- **订单号**（最近一笔订单号？）

💡 没拿到也没关系，回复「继续」我就基于现有信息先给您一份初步分析。
```

### T1.4 口播（10 秒）
> 「看，AI 不瞎编 —— 缺关键证据就反问，让运营或商户补全后再诊断。这是 Day 14 修的关键 bug：以前没 country/channel/error_code 时，AI 会一本正经胡编一个答案骗商户。」

**闭嘴 2 秒**

---

## T2 · 揭示简报已落回 DM（1:00 → 1:20）

### T2.1 操作

滚轮向上，回到 T0 创建工单那条气泡，看「💼 智能交接简报」

### T2.2 口播（15 秒）
> 「**重点 —— T0 那条 5 段简报**。生产环境是通过 `send_private` 私发给运营 lead（商户看不到），演示用一个账号跑，所以简报也落回我的 DM。**lead 接手即掌握 5 段全貌**：商户、问题、AI 诊断、已尝试、还缺、下一步。这就是'机器人 + 人工'协作 —— 不是替代，是接力。」

**闭嘴 5 秒**（鼠标悬停简报 5 秒）

---

## T3.0 · 商户「需要」延续会话 → 自动创建工单（1:20 → 1:35）

### T3.0.1 商户回复（复制粘贴）

```
需要
```

### T3.0.2 等待 AI 处理（2 秒，闭嘴）

### T3.0.3 你看到的回复

```
🤝 为了给您更专业的服务，我帮您转接 **财务团队-争议处理** 专家跟进。

📨 **工单号**：`tkt_yyyyyyyyyyyy`
⏱️ 预计 12h 内回复您。
```

### T3.0.4 口播（10 秒）
> 「**多轮对话记忆 + 智能意图识别** —— 商户回「需要」（不是「帮我创建工单」），bot **不**问「请提供完整信息」，而是延续 T0 的上下文自动创建工单。用了 force_intent 机制强制覆盖意图分类，避免了'需要'关键词全不命中的问题。」

**闭嘴 3 秒**

---

## T3.1 · 商户「需要人工」自然语言触发（1:35 → 1:50）

### T3.1.1 商户发问（复制粘贴）

```
需要人工
```

### T3.1.2 等待 AI 处理（2 秒，闭嘴）

### T3.1.3 你看到的回复

```
� 为了给您更专业的服务，我帮您转接 **财务团队-争议处理** 专家跟进。

📨 **工单号**：`tkt_zzzzzzzzzzzz`
⏱️ 预计 12h 内回复您。
```

### T3.1.4 口播（10 秒）
> 「**自然语言触发转人工** —— 商户说「需要人工」，bot 创建工单并把简报发给团队 lead。**生产环境 lead 收到后点击简报里的跳转按钮回到原对话接管**。」

**闭嘴 3 秒**

---

## T3.2 · 人工客服接管（`[人工]` 前缀豁免）（1:50 → 2:15）

### T3.2.1 演示者切角色（手离开键盘 1 秒）

**口播**：「现在我切角色扮演人工客服接管。bot 设计了一个 `[人工]` 前缀豁免 —— 加这个前缀的会话直接调 TRA 关单，**不**让 AI 二次自动答。」

### T3.2.2 人工接管（复制粘贴，**不带 ticket_id**）

```
【人工】已解决
```

### T3.2.3 等待 AI 处理（2 秒，闭嘴）

### T3.2.4 你看到的回复

```
👨‍💼 **人工客服已接管** 工单 `tkt_zzzzzzzzzzzz`

📝 处理说明：已解决

✅ 工单状态：**已关闭**（status=closed）

� 知识沉淀：自动升格为 FAQ（faq_case_tkt_xxxx_xxxx）
```

### T3.2.5 口播（15 秒）
> 「**核心创新 —— 人工接管用前缀豁免实现**。商户说「【人工】已解决」（**不**带 ticket_id），bot 不问「请提供工单号」，而是**自动从会话状态找最近工单**关掉。这就是'机器人 + 人工'协作 —— AI 兜底，人工升级，闭环。」

**闭嘴 5 秒**（让观众读关单确认）

> 💡 **复制 case_id**：从「🧠 知识沉淀」行复制 `faq_case_tkt_xxxx_xxxx`（T4.5 用）

---

## T4 · KEA 待审核列表（2:15 → 2:30）

### T4.1 商户群发（复制粘贴）

```
列出待审核的 case
```

### T4.2 等待 AI 处理（2 秒，闭嘴）

### T4.3 你看到的回复

```
📋 待审核案例（共 5 条）：

1. `faq_case_tkt_xxxx_xxxx`
   问题：拒付 · Visa 13.1
   置信度：0.85 · 自动入审

2. `faq_case_tkt_yyyy_yyyy`
   ...

💡 回复「✅ case_xxx」通过审核 /「❌ case_xxx」拒绝
```

### T4.4 口播（10 秒）
> 「半自动闭环 —— AI 不能直接进知识库，必须人工把关。这是企业级 AI 的合规红线。T3.2 刚生成的 case 已经在列表里了。」

**闭嘴 2 秒**

---

## T4.5 · 审核通过 → 入库（2:30 → 2:45）

### T4.5.1 审核通过（复制粘贴，**用 T3.2 复制的真实 case_id**）

```
✅ faq_case_tkt_xxxx_xxxx
```

### T4.5.2 等待 AI 处理（2 秒，闭嘴）

### T4.5.3 你看到的回复

```
✅ `faq_case_tkt_xxxx_xxxx` 已通过审核，已加入知识库
当前 faq_vec 共 N 条（之前 M 条）
```

### T4.5.4 口播（5 秒）
> 「知识库 +1。**注意数字 —— 之前 M，现在 N。这就是数据飞轮**：每审一条，知识库就长大一点。」

---

## T5 · 飞轮验证（2:45 → 3:15）

### T5.1 商户群发（复制粘贴）

```
BR Visa 13.1 又拒付了
```

### T5.2 等待 AI 处理（3 秒，闭嘴）

### T5.3 你看到的回复

```
🤖 AI 诊断：

� 命中知识库（上次同类问题已解决）
   引用：`faq_case_tkt_xxxx_xxxx`
   根因：拒付 · Visa 13.1
   置信度：高（85%）

📋 证据链：
- faq_vec 命中 1 条
- error_codes_vec 命中 3 条

🎯 建议下一步：直接复用已沉淀方案
```

### T5.4 口播（15 秒）
> 「**第二次问同问题，AI 直接复用 T3.2 沉淀的 FAQ —— 数据飞轮效应**。**T0 时 KEA 召回 0 条**（FAQ 还没这条），**T5 时召回 1 条**（刚审的 case 已沉淀）。同问题不再从零开始。这不是预设脚本，是 Chroma 真实召回。」

**闭嘴 5 秒**

---

## T6 · Dashboard 收尾（3:15 → 3:40）

### T6.1 浏览器切到 Dashboard

`Alt+Tab` 切到浏览器 → 打开 `https://oceanmate.feishu.cn/base/LQkTbJe1jaCM1PsFU48cslfgnSe`

### T6.2 口播（20 秒，配合鼠标停顿）
> 「数据规模 —— **错误码库 117 条、支付方式 16 条、路由规则 10 条**，**总 143 条真实数据，全部从飞书多维表来，不是 mock**。**faq_vec 当前 N 条**（每次录屏会变）—— Chroma 自进化。**测试覆盖 434 passed**。」

---

## 🎯 收尾（3:40 → 4:00）

### END.1 切回飞书 OM AI 私聊

`Alt+Tab`

### END.2 结尾总结（口播，20 秒）
> 「**8 段数字员工闭环**：
> 1. **机器人自动诊断** —— PDA + LLM + Chroma 召回
> 2. **自动派单** —— TRA + 路由规则匹配 + 工单创建
> 3. **AI 简报私发** —— 5 段交接简报给团队 lead
> 4. **反问机制** —— 缺关键证据不瞎编
> 5. **会话延续 + force_intent** —— 「需要」也能续上下文
> 6. **自然语言转人工** —— 「需要人工」自动派单
> 7. **人工接管** —— `[人工]` 前缀豁免 + session 找最近 ticket
> 8. **数据飞轮** —— 关单自动沉淀 → 复用召回
>
> **3 个亮点**：
> 1. **不是 AI 客服，是 AI 数字员工** —— 商户界面看不到内部简报
> 2. **数据飞轮自进化** —— 每次审核 +1，召回命中率涨
> 3. **PoC 不是演示，是生产级** —— 飞书 WS 真接通 + 143 条真实数据 + 434 测试 + 真实 lead open_id
>
> **PoC 阶段能演示的才是功能**。完。」

### END.3 停止录屏

`Win + Alt + R`（小红点消失）

---

## 4. 失败兜底

| 失败 | 兜底 |
|------|------|
| bot 不回 | 看 `/tmp/uvicorn_day18.log` `[WS]` 段，有无报错 |
| briefing 没落 DM | `.env` 里 `FEISHU_TEAM_*_OPEN_ID` 必须全是 lead 自己的 open_id（见 `.env`） |
| `【人工】已解决` 不生效 | 看 `[WS] [人工] 前缀不带 ticket_id` 日志；确认前缀是 `【人工】`（全角）或 `[人工]`（半角）|
| `【人工】已解决` 没自动找 ticket | 看 `[WS] [人工] 前缀不带 ticket_id，从会话状态找到最近` 日志；如果没日志，检查上一轮是否成功创建工单 |
| T3.0 「需要」走 MSA 反问 | 看 `[Orchestrator] force_intent=ticket_routing` 日志；如果没日志，说明会话延续没生效 |
| T3 工单号找不到 | 用 T0 飞书回复里的真实 `tkt_xxxxxxxxxxxx`（每次跑会变）|
| case_id 没生成 | T3.2 关单响应里会显示 `faq_case_xxx`，复制即可 |
| T3.2 关单后 status=pending 而不是 closed | 看 `/tmp/uvicorn_day18.log` `[WS] [人工] 接管` 段；确认消息以 `【人工】` 或 `[人工]` 开头 |

---

## 5. 实时核对清单

每完成 T 打 ✅：

| T | 视觉确认 |
|---|----------|
| 开场 | 飞书 OM AI 私聊窗口可见 |
| T0 | 3 条气泡（诊断 + 简报 + 工单确认） |
| T1 | AI 反问列表（4 项） |
| T2 | 滚回 T0 看到 5 段简报 |
| T3.0 | 「需要」→ 创建新工单 |
| T3.1 | 「需要人工」→ 创建工单 + 简报 |
| T3.2 | 「【人工】已解决」→ 「👨‍💼 人工接管」+ 关单 + KB 沉淀 |
| T4 | 待审列表含 T3.2 case |
| T4.5 | faq_vec +1 |
| T5 | 「⚡ 命中知识库」+ case_id |
| T6 | Dashboard 4 张表 |
| END | OM AI 私聊画面 + 结尾口播 |

---

## 6. 8 段闭环对应代码路径

| T | 用户动作 | 代码路径 |
|---|----------|----------|
| T0 | 发问题 | WS → Orchestrator → PDA/TRA/KEA 链式 → send_message + send_private(briefing) |
| T1 | 发缺信息问题 | PDA `confidence < 0.7` → 反问模板 |
| T2 | 看简报 | 已在 T0 自动推（无需切窗口） |
| T3.0 | 发「需要」（肯定词） | ws_client session 延续 + Orchestrator force_intent → TRA route_ticket |
| T3.1 | 发「需要人工」 | Orchestrator 关键词命中 ticket_routing → TRA route_ticket |
| T3.2 | 发「【人工】已解决」 | ws_client.py 前缀豁免 + session 找最近 ticket → TRA.resolve_ticket + KEA.promote_to_faq |
| T4 | 发「列出待审」 | KEA `list_candidates` |
| T4.5 | 发「✅ case_xxx」 | KEA `approve_case` |
| T5 | 同问题再问 | PDA 召回 faq_vec 命中 |
| T6 | 浏览器 Dashboard | 飞书多维表格实时数据 |

---

## 7. Day 18 P1 修复清单（录屏里要演示的核心创新）

| # | 修复 | 文件 | 验证 |
|---|------|------|------|
| 1 | `[人工]` 前缀豁免（半角+全角）| `ws_client.py` | T3.2 |
| 2 | session 状态延续（5 轮滑动窗口）| `ws_client.py` | T3.0 |
| 3 | force_intent 强制覆盖意图 | `orchestrator.py` | T3.0 |
| 4 | session 找最近 ticket | `ws_client.py` | T3.2 |
| 5 | 10 个 team open_id 全 self-DM | `.env` | T0 briefing |
| 6 | main.py 显式 logging.basicConfig | `main.py` | 日志可看 |
| 7 | TRA problem_type 自动推断 | `tra/tool.py` | T0 缺参数场景 |
| 8 | routers CREATE 关键词优先 | `routers.py` | 边界场景 |

---

## 8. 录屏前最后 60 秒预演清单

- [ ] 后端 PID 27436 在跑
- [ ] WS state 干净（events_received=0）
- [ ] 飞书 OM AI 私聊窗口已开
- [ ] 飞书通知已关闭
- [ ] dry-run 跑过，参考 ID 已知
- [ ] 录屏工具麦克风图标显示
- [ ] 浏览器 Dashboard 标签已开（T6 用）

**预演一次**（不要录屏）：把 T0-T5 在飞书跑一遍，看链路通畅。**确认无错再开始录屏**。
