# OceanMate AI · 主 Demo 录屏脚本 v4（4 分钟 · 单账号复制粘贴版）

> **生成时间**：2026-08-16（截止日当天 · 最终版 · v4）
> **演示载体**：飞书 OM AI 私聊（单窗口，全程不切换）
> **单账号限制**：飞行社账号 = 商户 + 人工客服（同一人）
> **Backend**：`:8000` LISTENING（最新 v15，含「自动入审 + 多维表格 review_decisions 同步」）
> **核心创新**：机器人自助优先 + AI 答不了硬触发 + 人工模式状态机 + 自动入审 + **真实多维表格数据飞轮**

---

## v4 vs v3 差异（重点变更）

| # | 变更 | 原因 |
|---|------|------|
| 1 | **删除 T5「列出待审」段** | 商户没义务审核自己的 case，**审核必须由运营在后端完成**（用户反馈 8-16）|
| 2 | **删除 T6「✅ case_xxx 审核」段** | 同上 — 不在飞书聊天里审核，应该在多维表格/后端做（用户反馈 8-16）|
| 3 | **T3.14 case_id 不再需要复制** | 没有 T6 用了，case_id 只用于多维表格展示 + 入审时间戳 |
| 4 | **T8 升级为「数据飞轮 · 多维表格实测」** | 把审核搬到多维表格（运营侧） — 对话结束后切到飞书多维表格，展示 T3.14 的自动入审记录（含时间戳）|
| 5 | **增加 T8.2「待审核区」+T8.3「review_decisions 表」** | 录屏关键创新点 — 让评审看到**真实数据飞轮**：商户提问 → bot 自动入审 → 同步多维表格 → 后续同类问题命中 |
| 6 | **T7「飞轮验证」保留** | 仍演示 faq_vec 召回真实增加（半自动闭环的产物）|

---

## §0 录屏前 60 秒准备

| # | 操作 | 命令/动作 |
|---|------|-----------|
| 1 | 后端已起 | `curl http://localhost:8000/api/health` → 200 + 4 Tool |
| 2 | WS 健康 | `curl http://localhost:8000/api/debug/ws_state` → `thread_alive: true` |
| 3 | WS 静默测试 | 飞书发「测试」，bot 回 ≥ 1 条 |
| 4 | 人工模式 | `curl http://localhost:8000/api/debug/human_mode` → `users_in_human_mode: []` |
| 5 | 飞书窗口 | 左侧栏 → OM AI → DM |
| 6 | 关闭通知 | 飞书设置 → 通知 → 关闭 |
| 7 | 浏览器 Dashboard | `https://oceanmate.feishu.cn/base/LQkTbJe1jaCM1PsFU48cslfgnSe` 标签页开好 |
| 8 | 多维表格 · 待审核区 | 浏览器开 `review_decisions` 表的"T8 用视图" |
| 9 | 开始录屏 | `Win + Alt + R`（右下角小红点出现） |

⚠️ **录屏全程不要打开 DevTools / 新标签**。
⚠️ **预演 1 次**（不录屏）：T0~T7 在飞书跑一遍，无错再开始录。

---

## §1 📋 复制粘贴清单（所有要发给飞书的内容，按时间顺序）

> **录屏时**直接从这里复制 → 鼠标点飞书输入框 → Ctrl+V → Enter。

### 1.1 商户发送（按时间段）

| 段 | 复制粘贴（点击发送） | 角色 |
|----|---------------------|------|
| T0.1 | `BR Visa 拒付，错误码 13.1，怎么办？` | 商户 |
| T1.1 | `NL MasterCard 拒付，没收到错误码` | 商户 |
| T1.5 | `错误码是 13.1，订单号 ORD-12345。物流凭证和 RDR 都已提交` | 商户 |
| T2.1 | `试了没用，运营团队帮我跟进一下` | 商户 |
| T3.2 | `【人工】接手工单 <T2.3 工单号>` | **人工**（演示者） |
| T3.6 | `Visa 那边怎么说？` | 商户（测试 bot 静默） |
| T3.8 | `已查清：Visa 风控误判，帮你申诉成功，问题已解决。还有问题吗？` | **人工**（演示者） |
| T3.11 | `没了，谢谢` | 商户（确认） |
| T3.12 | `【人工】退出人工模式` | **人工**（演示者） |
| T7.1 | `BR Visa 13.1 又拒付了` | 商户 |

> ⚠️ **角色说明**：
> - **商户身份**发：T0.1 / T1.1 / T1.5 / T2.1 / T3.6 / T3.11 / T7.1
> - **人工身份**发：T3.2 / T3.8 / T3.12（演示者从同一账号发出，飞书界面靠发送者标签区分 "我" vs bot）
>
> ⚠️ **占位符**：
> - T3.2 的 `<T2.3 工单号>` — **先用 §2 跑完 T2.3 → 飞书回复里有 `tkt_xxxxxxxxxxxx` → 复制那段工单号**替换后发送

### 1.2 不需要发送的演示动作

| 段 | 动作 |
|----|------|
| T2.4 | 滚轮向上 → 看简报气泡 5 段 + 跳转链接 |
| T3.7 | 等 5 秒看 bot 是否静默 |
| T3.14 | 复制 case_id 备用（v4 不再需要，但保留以便回看）|
| T8.1 | `Alt+Tab` 切到浏览器 Dashboard |
| T8.2 | 鼠标移到「`review_decisions` 待审核区」表 |
| T8.3 | 滚动到底部 → 找 T3.14 自动入审的那条（最新一条）|
| T8.4 | 鼠标悬停在「决策时间」列 3 秒（展示真实时间戳）|
| END.1 | `Alt+Tab` 切回飞书 |
| END.3 | `Win + Alt + R` 停止录屏 |

---

## §2 🎬 分段执行脚本（含口播）

### 🎬 开场（0:00 → 0:15）

**口播**（13 秒）：
> 「这是 OceanMate AI —— 跨境支付商户的**数字员工**，不是客服机器人。**机器人 + 人工协作的完整闭环**。接下来 4 分钟演示 8 段：机器人自助诊断 → 反问机制 → 追问闭环 → AI 答不了触发派单 → 简报私发 lead → 人工模式状态机 → 退出 + 关单 + 自动入审 → 多维表格实测数据飞轮。」

**闭嘴 2 秒**（让观众看清飞书窗口）

---

### T0 · 机器人自助诊断（0:15 → 0:45）—— 30 秒

**T0.1** 复制粘贴发送：`BR Visa 拒付，错误码 13.1，怎么办？`

**T0.2** 等待 3-5 秒（闭嘴）

**T0.3** 你看到的回复（机器人自助方案 · 不自动派单）：

```
🤖 AI 诊断：

💳 渠道+错误码：**Visa 13.1**
   含义：持卡人/银行认为未收到商品 / 服务

🔍 根因：商户未在 RDR 前提供物流凭证

📋 证据链：
- error_codes_vec 命中 3 条
- faq_vec 命中 1 条

🎯 建议下一步：
1. 补充物流凭证（tracking number + delivery proof）
2. 提交 RDR（Retrieval Request）申请
3. 如已 RDR 仍被拒，再来反馈
```

**T0.4 口播**（10 秒）：
> 「**关键设计 —— 机器人自助优先**。bot 答得了的问题**不自动派单**，直接给方案：Visa 13.1 的根因 + 证据链 + 处置 3 步。前一版本会一键派单找人工，**违反产品逻辑** —— 机器人答得了为什么还打扰运营？这条我们 Day 18 禁用了 PDA→TRA 链式触发。」

**闭嘴 3 秒**

---

### T1 · 反问机制 + 追问闭环（0:45 → 1:25）—— 40 秒

**T1.1** 复制粘贴发送：`NL MasterCard 拒付，没收到错误码`

**T1.2** 等待 3 秒（闭嘴）

**T1.3** 你看到的回复（**软反问 · 1 项 · 不机械列 4 项**）：

```
🤔 我需要 1 个关键信息才能准确诊断：

❓ **错误码是什么？**（Visa/Mastercard 拒付通常有 4 位数字错误码）

补上我就能给出完整方案。
```

**T1.4 口播**（8 秒）：
> 「**反问机制** —— bot 明确说**缺什么**，**1 项不机械列 4 项**。前一版本会说'缺国家/渠道/错误码/订单号'，吓跑商户。**少即是多**，只问当前 case 实际缺的那 1 项。」

**闭嘴 2 秒**

**T1.5** 复制粘贴发送：`错误码是 13.1，订单号 ORD-12345。物流凭证和 RDR 都已提交`

**T1.6** 等待 3 秒（闭嘴）

**T1.7** 你看到的回复（**评估够了 → 5 段完整诊断**）：

```
🤖 AI 诊断：

💳 渠道+错误码：**Mastercard 13.1**
   含义：持卡人/银行认为未收到商品 / 服务

🔍 根因：已提交 RDR 但仍被拒 → Visa 风控误判？

📋 证据链：
- error_codes_vec 命中 3 条
- faq_vec 命中 1 条
- cases_vec 命中 1 条（RDR 后仍被拒的相似 case）

🎯 建议下一步：
1. 准备 Visa 风控申诉材料（tracking + delivery proof）
2. 联系 Visa acquirer 走申诉流程
3. 7 个工作日内反馈
4. 如有 chargeback fee，可申请减免
```

**T1.8 口播**（8 秒）：
> 「**追问闭环** —— 商户补什么 bot 评估够了再答。**不丢票**（PD fallback），但**也不抢答**（LLM 之前是直接给方案，没看商户补充）。Day 18 P2 修复了 session 恢复 + 跨 channel 模板兜底，覆盖了商户说 MC 但 error_code 是 Visa 码的边界场景。」

**闭嘴 3 秒**

---

### T2 · AI 答不了硬优先级 + 智能交接简报（1:25 → 1:55）—— 30 秒

**T2.1** 复制粘贴发送：`试了没用，运营团队帮我跟进一下`

**T2.2** 等待 3 秒（闭嘴）

**T2.3** 你看到的回复（**TRA 派单 + 工单号 · 必复制**）：

```
🤝 AI 答不了 → 自动派单：

📨 **工单号**：`tkt_xxxxxxxxxxxx`
👥 **分派**：技术团队 L2（high）
⏱️ **SLA**：12h
🔗 **快速跳转**：https://applink.feishu.cn/client/chat/open?openChatId=oc_xxxxx
```

**T2.4** 滚轮向上 → **5 段简报气泡**（私发到 lead，演示单账号所以也落回这）：

```
📋 **智能交接简报**（技术团队 L2）

━━━━━━━━━━━━━━━━━━━━
1️⃣ **商户**：BR 跨境电商
2️⃣ **问题**：Mastercard 13.1 拒付，已提交 RDR 仍被拒
3️⃣ **AI 诊断**：根因=RDR 后仍被拒/疑似 Visa 风控误判，置信度 0.72
4️⃣ **已尝试**：物流凭证 + RDR
5️⃣ **下一步**：准备 Visa 风控申诉 / 联系 acquirer

📨 **工单**：`tkt_xxxxxxxxxxxx` | high | SLA **12h**
🔗 **快速跳转**：https://applink.feishu.cn/client/chat/open?openChatId=oc_xxxxx
```

**T2.5 口播**（15 秒）：
> 「**关键 —— AI 答不了硬优先级**。商户说'试了没用'，bot 不再纠结'怎么还没解决'，**直接派单**。**简报走 send_private** 到团队 lead（真实场景 lead 收到 DM，演示里 lead == 商户单账号所以也落回这）。简报末尾带 **跳转链接**，lead 点一下唤起飞书直接跳回这条对话接管。」

> 「**为什么是 5 段而不是 3 段**：商户、问题、AI 诊断、已尝试、下一步。运营看 30 秒就能接手。」

**闭嘴 3 秒**（鼠标悬停跳转链接 2 秒）

---

### T3 · 人工模式状态机 + 自动入审（1:55 → 2:25）—— 30 秒

> 💡 **设计说明**：本段演示"人工立即解决"的真实产品流程 —— 人工发 `[人工]` 前缀接管 → **立刻**解决问题（无需商户问）→ 商户**立即**确认 → 人工退出。**退出后 bot 自动入审到多维表格**（**核心创新点** —— 商户不审，运营在后端审）。

**T3.1** 演示者口播（手离开键盘 1 秒）：
> 「现在演示**人工模式**：人工发 `[人工]` 前缀进入 → 在场期间 bot **一律静默** → 人工立即解决 → 商户立即确认 → 人工退出。」

**T3.2** 复制粘贴发送（**用 T2.3 复制的真实工单号替换占位符** · **人工角色**）：

```
【人工】接手工单 tkt_xxxxxxxxxxxx
```

**T3.3** 等待 2 秒（闭嘴）

**T3.4** 你看到的回复（人工接管确认 · **5 分钟承诺**）：

```
👨‍💼 **人工客服已接管** 工单 `tkt_xxxxxxxxxxxx`

⏱️ **预计 5 分钟内**回复您（人工模式 · 不是 SLA 12h）
```

**T3.5** 演示者口播 3 秒（角色切换提示）：
> 「切角色。**演示'bot 静默'** —— 商户在人工模式下追问，bot 一字不回。」

**T3.6** 复制粘贴发送（**商户角色** · 测试 bot 静默）：

```
Visa 那边怎么说？
```

**T3.7** 等 5 秒（闭嘴 · **bot 应不回**）

> ⚠️ 商户问完「Visa 那边怎么说」后，**bot 一字不回**。如果 bot 回了，说明状态机没生效，去看 `/tmp/backend.log` `[WS] 人工模式静默` 日志。

**T3.8** 复制粘贴发送（**演示者切回人工角色** · 立即解决）：

```
已查清：Visa 风控误判，帮你申诉成功，问题已解决。还有问题吗？
```

**T3.9** 等 2 秒（闭嘴）

**T3.10** 屏幕显示（**人工回复** · 发送者标签是「我」**不是**「OceanMate AI」）

**T3.11** 演示者切商户角色（复制粘贴发送）：

```
没了，谢谢
```

**T3.12** 演示者切人工角色（复制粘贴发送）：

```
【人工】退出人工模式
```

**T3.13** 等 3 秒（闭嘴）

**T3.14** 你看到的回复（人工退出 + 关单 + **自动入审** · bot 已退出会话）：

```
👨‍💼 **人工客服已退出** 工单 `tkt_xxxxxxxxxxxx`

📝 处理说明：Visa 风控误判，已申诉成功

✅ 工单状态：**已关闭**（status=closed）

🧠 知识沉淀：自动入审，case_id `case_tkt_xxxx_xxxx`
   决策：`pending_review`（置信度 0.85，等运营审核）
   时间：2026-08-16T02:35:22+08:00
```

> 💡 **关键**：bot 不再需要复制 case_id — **审核不在聊天里做**，运营在多维表格后端做（T8 展示）。

**T3.15 口播**（15 秒）：
> 「**三件事一起做了**：1) 关单（TRA resolve_ticket）；2) **bot 自动入审**（KEA promote_to_faq → 进 `pending_review` 状态，**置信度 0.85 中等，不直接进知识库**）；3) **同步写入飞书多维表格**（`review_decisions` 表，含真实时间戳）。**半自动闭环** —— AI 没权限直接进知识库，必须运营审。这是企业合规红线。」
>
> 「**核心 —— bot 全程静默**：商户问 `Visa 那边怎么说`，bot 一字不回。**人工客服主动出击**：接单 → 立即解决 → 主动反问 → 商户确认 → 退出。bot 全程不抢话。」

**闭嘴 3 秒**

---

### T4 · (空段 · 原 T4 已合并到 T3) —— 0 秒

> 💡 **v4 简化**：原 v3 的 T4（退出 + 关单 25s）已合并到 T3 的 30s 段。v4 没有单独的 T4 段。

---

### T7 · 飞轮验证：同问题再问（2:25 → 2:45）—— 20 秒

> ⚠️ **录屏注意**：T7 演示**部分飞轮** —— 因为没经过运营审核，`pending_review` 的 case 还没进 `faq_vec`，召回数应该和 T0 一样（仍 1 条历史）。**真正的数据飞轮**在 T8 展示（多维表格里有 `pending_review` 记录 + 时间戳）。

**T7.1** 复制粘贴发送：

```
BR Visa 13.1 又拒付了
```

**T7.2** 等待 3 秒（闭嘴）

**T7.3** 你看到的回复（**基础召回 · 历史 faq_vec**）：

```
🤖 AI 诊断：

💳 渠道+错误码：**Visa 13.1**

🔍 根因：拒付 · Visa 13.1

📋 证据链：
- error_codes_vec 命中 3 条
- faq_vec 命中 1 条（历史 · T0 时也是 1 条）

🎯 建议下一步：
1. 补充物流凭证 + 申请 RDR
2. 如已提交仍被拒，联系 Visa 风控申诉
```

**T7.4 口播**（10 秒）：
> 「**部分飞轮验证** —— `faq_vec` 召回 1 条（T0 时也是 1 条），**没变**。因为 T3.14 的 case 还在 **`pending_review` 状态**，**运营还没审核**，所以还没进 `faq_vec`。这恰好印证了我们的合规设计 —— **AI 不能自己进库**。」
>
> 「**真正的飞轮在 T8 展示** —— 多维表格里有 T3.14 自动入审的记录，运营审核后才会真进 `faq_vec`，下次召回就 +1。」

**闭嘴 3 秒**

---

### T8 · 数据飞轮 · 多维表格实测（2:45 → 3:25）—— 40 秒（核心创新点）

> 💡 **录屏关键**：这是 **v4 vs v3 的核心差异** —— 不再是聊天里的"✅ case_xxx"假审核，而是**真实的多维表格后端审核**。录屏时鼠标操作要稳，让评审看清每条字段。

**T8.1** `Alt+Tab` 切到浏览器（飞书多维表格 `review_decisions` 表）

**T8.2** 鼠标停在 `review_decisions` 表的"待审核区"，口播（10 秒）：
> 「**真实数据飞轮 —— 商户侧对话 → bot 自动入审 → 同步多维表格**。我们不再让商户或聊天里审核自己的 case。**审核在多维表格后端完成**（运营视角，单账号演示下演示者手动模拟）。这张 `review_decisions` 表是**飞书真实多维表格**，不是 SQLite mock。」

**T8.3** 滚到底部，找到**最新一条**（T3.14 自动入审的 case）：

```
┌─────────────────────────────────────────────────────────┐
│ 案例ID              │ 决策         │ 审核人  │ 决策时间              │
├─────────────────────────────────────────────────────────┤
│ case_tkt_xxxx_xxxx  │ pending_review │ auto   │ 2026-08-16T02:35:22 │
│ 问题类型            │ 置信度       │ 工单ID                │
│ 拒付 · Visa 13.1   │ 0.85         │ tkt_xxxxxxxxxxxx      │
└─────────────────────────────────────────────────────────┘
```

**T8.4** 鼠标悬停「决策时间」列 3 秒（展示真实时间戳），口播（10 秒）：
> 「**字段解读** —— 案例 ID 是 T3.14 bot 回复里的 `case_tkt_xxxx_xxxx`，决策是 `pending_review`（置信度 0.85 中等，等人工审），审核人是 `auto`（bot 自动入审，不是人工），决策时间是 **ISO 格式 + 北京时区**。每条记录**实时写入**，不是预先塞好的。」

**T8.5** 口播继续（10 秒）：
> 「**完整闭环** —— 商户问 → PDA 答不了 → TRA 派单 → 人工接管 → 关单 → bot 自动入审（这一步在 v3 是聊天里审，v4 移到后端）→ 运营审核 → 进 faq_vec → 下次同类问题召回 +1。」
>
> 「**3 个产品亮点**：(1) **商户没审核义务** —— 审核是运营职责；(2) **审核不可绕过** —— AI 答对了也不能自己入库；(3) **数据可追溯** —— 多维表格有时间戳 + 审核人，事后审计能查。」

**闭嘴 3 秒**

---

### 🎯 收尾（3:25 → 3:40）—— 15 秒

**END.1** `Alt+Tab` 切回飞书 OM AI 私聊

**END.2** 结尾总结（口播，18 秒）：
> 「**8 段数字员工闭环**：
> 1. **机器人自助诊断**——PDA + LLM + Chroma 召回 + 给方案不派单
> 2. **反问机制**——bot 说缺什么，1 项不机械列 4 项
> 3. **追问闭环**——商户补什么，bot 评估够了再答（不丢票）
> 4. **AI 答不了硬触发**——「试了没用 / 我不懂」自动派单
> 5. **send_private 简报私发**——5 段 + 跳转链接，30 秒接手
> 6. **人工模式状态机**——[人工] 进 / 商户再问 bot 静默
> 7. **退出人工 + 关单 + bot 自动入审 + 同步多维表格**——半自动闭环起点
> 8. **多维表格实测**——真实数据飞轮展示
>
> **3 个亮点**：
> 1. **机器人自助优先**——答得了不派单，**不打扰运营**
> 2. **人机共存**——bot 静默不干扰人工接管
> 3. **审核后端化**——商户不审自己，运营在多维表格审，**合规可审计**
>
> **演示价值 > API 实现**。完。」

**END.3** `Win + Alt + R`（小红点消失）

---

## §3 ⏱️ 总时长核对

| 段 | 时长 | 累计 |
|----|------|------|
| 开场 | 15s | 0:15 |
| T0 | 30s | 0:45 |
| T1 | 40s | 1:25 |
| T2 | 30s | 1:55 |
| **T3（含原 T4）** | **30s** | 2:25 |
| T7 | 20s | 2:45 |
| **T8（多维表格实测）** | **40s** | 3:25 |
| 收尾 | 15s | 3:40 |
| **总计** | **3 分 40 秒** | |

> 💡 **段号变化**：v3 是 T0~T8 共 9 段（含 T5/T6 假审核）；v4 是 T0~T3 + T7 + T8 共 6 段（去 T4/T5/T6，新增 T8 多维表格实测）。**总时长不变 3:40**。

---

## §4 失败兜底

| 失败 | 兜底 |
|------|------|
| bot 不回 | 看 `/tmp/backend.log` `[WS]` 段，有无异常 stack |
| T0 自动派单（应不派） | 检查 `chain_config.py`：`PDA_TO_TRA_CHAIN.trigger = lambda _: False` |
| T1 反问没出 | 看 `[PDA] confidence < 0.7` 日志；query 必须缺 country/channel/error_code |
| T1.7 不给完整诊断 | **Day 18 P2-fix + P2-final 双修**：(1) ws_client session 恢复支持「商户补充事实类长句」→ country/channel 不丢；(2) `_PDA_TEMPLATES` 新增 `(拒付, not_received, Mastercard)` + `(拒付, not_authorized, Visa)` + channel=None 兜底，覆盖「商户说 MC 但 error_code 是 Visa 码」跨 channel 场景。验证：T1.5 后 bot 不再说默认"拒付对商户影响较大"，而是 "Mastercard 13.x 拒付（买家说没收到货）" |
| T2 商户说"试了没用"走 PDA 反问 | 看 `[Orchestrator] 命中 _AI_CANNOT_ANSWER` 日志；关键词是否全 |
| T2 没有简报气泡 | 看 `[WS] 交接简报` 日志；确认 `.env` `FEISHU_TEAM_*_OPEN_ID` 都设了 |
| T2.4 看不到跳转链接 | `_build_jump_link(chat_id, chat_type)` 应返回 `applink.feishu.cn/client/chat/open?openChatId=...` |
| T3 [人工] 前缀生效但 bot 不静默 | 检查 `ws_client.py` `_handle_p2_im_message` 的人工模式分支顺序 |
| T3.5 商户再问 bot 回了 | **严重 bug**：人工模式状态机失效，看 `[WS] 人工模式静默` 日志 |
| T3.12 关单后 status=pending | `[人工]` 前缀必须带或不带 ticket_id 都能解析，确认走的是 session fallback |
| T3.14 没自动入审 | 看 `[KEA] promote_to_faq` 日志；确认 status 是 closed |
| T3.14 多维表格没记录 | 检查 `.env` `FEISHU_BTABLE_REVIEW_DECISIONS_TABLE_ID` 已配；backend log 有 `[feishu] review_decision 同步成功` |
| T8.2 多维表格打不开 | 确认 `.env` 凭证齐全，浏览器先登录飞书工作台 |
| T8.3 找不到 T3.14 记录 | 按"决策时间"倒序排，最新一条就是；如时间不对看后端时区（应为 +08:00）|
| 简报干扰商会 DM | **已修复**：send_private 直接发，不拦截 |

---

## §5 实时核对清单

每完成 T 打 ✅：

| T | 视觉确认 | 打 ✅ |
|---|----------|--------|
| 开场 | 飞书 OM AI 私聊窗口可见 | ☐ |
| T0 | 1 条长气泡（自助诊断 · 不应派单）| ☐ |
| T1.3 | 软反问：bot 明确说「缺 error_code」1 项 | ☐ |
| T1.5 | 商户补「13.1 / ORD-12345」| ☐ |
| T1.7 | bot 评估够了 → 5 段完整诊断（根因 + 证据链 + 4 处置）| ☐ |
| T2.3 | TRA 派单气泡（含工单号 · **必复制**）| ☐ |
| T2.4 | 简报气泡 5 段 + 跳转链接 | ☐ |
| T3.4 | 人工接管确认（含「5 分钟内」承诺）| ☐ |
| T3.7 | 商户问「Visa 那边怎么说」→ **bot 不回**（**静默演示点**）| ☐ |
| T3.10 | 演示者（人工角色）发「Visa 风控误判，已帮你申诉成功」（**人工标签**）| ☐ |
| **T3.14** | **退出 + 关单 + 自动入审（含真实 case_id + 时间戳）**| ☐ |
| **T7.3** | **faq_vec 召回 1 条（与 T0 相同 · pending_review 未进库）**| ☐ |
| **T8.2** | **多维表格 review_decisions 表打开（不空）**| ☐ |
| **T8.3** | **找到 T3.14 那条（最新一条 · pending_review · auto）**| ☐ |
| **T8.4** | **决策时间格式：2026-08-16Txx:xx:xx+08:00**| ☐ |
| END | OM AI 私聊画面 + 结尾口播 | ☐ |

---

## §6 6 段闭环对应代码路径

| T | 用户动作 | 代码路径 |
|---|----------|----------|
| T0 | 发问题（4 关键信息全）| WS → Orchestrator → PDA 给方案（chain 禁用，不派单）|
| T1.1 | 发问题（缺 error_code）| PDA `confidence < 0.7` → 软反问 1 项（明确说缺什么）|
| T1.5 | 商户补全 | PDA 参数足够 → `_classify_intent → route_pda → safe_execute` |
| T1.7 | bot 评估够了 | PDA 输出：`problem_type`+`confidence`+`root_causes`+`recommendations`+配图 |
| T2.1 | 发「试了没用」| Orchestrator `_AI_CANNOT_ANSWER` 硬优先级 → TRA route_ticket → send_private(briefing + jump_link) |
| T3.2 | 发「[人工] 接手工单」| ws_client 3 步处理器 → 进 `_human_mode` |
| T3.6 | 商户在人工模式下追问 | ws_client `_is_in_human_mode` → return（不发 Orchestrator）|
| T3.8 | 演示者（人工）立即答 | **人工直接走飞书 UI**，不经过 bot |
| T3.11 | 商户立即确认 | 商户消息（人工在场下 bot 静默）|
| T3.12 | 发「[人工] 退出」| ws_client 退出模式 → TRA resolve_ticket + KEA promote_to_faq(case_id) + **frontend.sync_review_decision()** 写多维表格 |
| T7.1 | 同问题再问 | PDA Chroma 召回（pending_review 还没进库，召回数不变）|
| T8.1 | 浏览器多维表格 | 飞书 `review_decisions` 表 · 真实数据飞轮展示 |

---

## §7 Day 18 P1-final 修复清单（录屏里要演示的核心创新）

| # | 修复 | 文件 | 演示点 |
|---|------|------|--------|
| 1 | 机器人自助优先（禁 PDA→TRA 链式）| `chain_config.py` | T0（不派单）|
| 2 | AI 答不了硬优先级 | `orchestrator.py` | T2（"试了没用"→派单）|
| 3 | TRA 自然语言触发（运营团队/介入/跟进）| `orchestrator.py` + `routers.py` | T2 边界场景 |
| 4 | 人工模式状态机（进/退/静默/超时）| `ws_client.py` | T3（静默）/ T3.12（退出）|
| 5 | `_build_jump_link` 跳转链接 | `ws_client.py` | T2.4 |
| 6 | send_private 直接发（不拦截）| `ws_client.py` | T2 单账号演示 |
| 7 | main.py 显式 `logging.basicConfig` | `main.py` | 全程日志可看 |
| 8 | **T3.14 bot 自动入审到多维表格**（**v4 核心**）| `kea/tool.py:_sync_review_decision_to_bitable` + `feishu/frontend.py:sync_review_decision` | T3.14 + T8 |
| 9 | `/api/debug/human_mode` 调试端点 | `main.py` | 录屏前自检 |
| 10 | **list_candidates 加 created_at**（运营审核能看到入审时间）| `kea/tool.py:_list_candidates` | T8 多维表格 |
| 11 | **approve_case 同步多维表格**（运营点击后写记录）| `kea/tool.py:_approve_case` | T8（运营手动审核场景）|

---

## §8 录屏前最后 60 秒预演清单

- [ ] 后端在 `:8000` LISTENING（`netstat -ano | grep :8000`）
- [ ] WS thread alive（`/api/debug/ws_state`）
- [ ] WS events_received=0（清状态重置）
- [ ] `/api/debug/human_mode` 为空（无人人工模式）
- [ ] `/api/debug/briefings` 为空（新流程开始）
- [ ] 飞书 OM AI 私聊窗口已开
- [ ] 飞书通知已关闭
- [ ] 浏览器 Dashboard 标签已开（T8 用）
- [ ] 多维表格 `review_decisions` 表标签已开
- [ ] 录屏工具麦克风图标显示

**预演 1 次**（不录屏）：T0~T8 在飞书 + 多维表格跑一遍，链路通畅 + 静默生效 + 简报有链接 + 自动入审 + 多维表格写入。

**确认无错再开始录屏**。